fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
geoalchemy2==0.16.0
python-dotenv==1.0.1
pydantic-settings==2.7.1
sentry-sdk[fastapi]==2.19.2

# Auth (P2): password hashing + JWT. bcrypt pinned <4.1 — newer releases
# dropped the __about__.__version__ attribute passlib's bcrypt backend
# reads to detect the installed version.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1
slowapi==0.1.10
zxcvbn==4.5.0

# Test tooling (pulled forward from T13 — T10's lifecycle tests need pytest
# to run; T13 adds the fuller conftest.py/DB-fixture scaffolding on top).
pytest==8.3.4
httpx==0.28.1

# Lint (T14 backend CI)
ruff==0.15.20