// lib/fonts.ts
// Shared font-family string literals for the homepage/marketing screens,
// which set `fontFamily` directly via inline styles rather than Tailwind's
// font utility classes. Both fonts are already loaded via the @import at
// the top of app/globals.css.

export const FONT_HEADING = "'Space Grotesk', sans-serif";
export const FONT_BODY = "'Plus Jakarta Sans', sans-serif";
